{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "DCRec Train",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "args": [
                "--use_memory",
                "--prefix", "tgn-attn-compare",
                "--n_neg", "10",
                "--train_ratio", "0.05",
                "--valid_ratio", "0.01",
                "--n_epoch", "20",
                "--n_skip_val", "0",
                "--randomize_features"
                // "--noise_pruning_ratio", "0.5"
                // "--batch_size", "128"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        },
        {
            "name": "Common Debug",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": false,
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}